# TensorFlow Setup Note

TensorFlow is required for the LSTM forecasting step in Module 4.

The project uses Python 3.11 inside `venv_bigdata` for compatibility with the practicum dependency set.

Verification command:

```bash
python -c "import tensorflow as tf; print(tf.__version__)"
```

